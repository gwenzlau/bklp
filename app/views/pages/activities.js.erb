
$boxes = $('<%= j render(@activities) %>')

$('#activities').append( $boxes );
<% if @activities.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate(@activities) %>');
<% else %>
    $('.pagination').remove();
<% end %>

$('#activities').append('<%= j render (@activities) %>');
$('.pagination').replaceWith('<%= j will_paginate(@activities) %>');




/*
$('#activities').append('<%= j render(@activities) %>');
<% if @activities.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@activities) %>');
<% else %>
$('.pagination').remove();
<% end %>*/