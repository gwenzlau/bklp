joined bookloop!
<% if activity.trackable %>
	<%= link_to Book.find_by_id(activity.trackable.id).title, user_path(User.find_by_id(activity.trackable.id).olidb) %>
<% else %>
	a book which has since been abandoned.
<% end %>