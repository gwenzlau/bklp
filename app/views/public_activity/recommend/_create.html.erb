recommended
<% if activity.trackable %>
	<% if Recommend.find_by_id(activity.trackable.id) %>
		
		<% recco = Recommend.find_by_id(activity.trackable.id) %>
		<%= link_to Book.find(recco).title, book_path(recco) %> 
	<% else %>
		the author
	<% end %>
<% end %> 