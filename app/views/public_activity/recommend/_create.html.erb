recommended
<% if activity.trackable %>
	<% if Recommend.find_by_id(activity.trackable.id).item_type == "book" %>
		the book
		<% recco = Recommend.find_by_id(activity.trackable.id) %>
		<%= link_to Book.find_by_olidb(recco.item_id).title, book_path(recco.item_id) %>
	<% else %>
		an
		<% recco = Recommend.find_by_id(activity.trackable.id) %>
		<% client = Goodreads.new(Goodreads.configuration) %>
		<% aut = client.author(7) %>
		<%= link_to aut.name, author_book_path(recco.item_id) %>
	<% end %>
<% end %>