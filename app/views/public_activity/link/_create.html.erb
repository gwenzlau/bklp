posted an article
<span class="article-feed">
<% if activity.trackable %>
	<%= link_to Link.find_by_id(activity.trackable.id).title, Link.find_by_id(activity.trackable.id).source, :target => "_blank" %>.
	</span>
	<span class="feed-sub">
	<small><br>
 	   <%= link_to "Read", (Link.find_by_id(activity.trackable.id).source), target: "_blank" %> |
	   <%= link_to "Discuss", link_path(Link.find_by_id(activity.trackable.id)) %> | 
	<% if Link.where(:user_id => current_user.id).where(:id => params[:id]).exists? %>
	<% if @mylink[0].status == 2 %>
	<% else %>
	<%= form_for @newlink, :url => {:action => "links#futurelink_list"} do |f| %>
				<%= f.hidden_field :title, :value => @link_title %>
				<%= f.hidden_field :source, :value => "" %>
				<%= f.hidden_field :status, :value => "2" %>
				<%= f.hidden_field :user_id, :value => current_user.id %>
				<%= f.submit "Save", :class => "btn" %>
			<% end %>
		<% end %>
	<% end %>
		</a>
			| <a href="#" id="al-<%= activity.id %>">Comment</a>
	  </small>
	</span>

<% else %>
	which has since been deleted.
<% end %>

