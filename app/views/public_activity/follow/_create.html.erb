started to follow the great reader, <%= link_to User.find_by_id(Follow.find_by_id(activity.trackable.id).followable_id).name, user_path(User.find_by_id(Follow.find_by_id(activity.trackable.id).followable_id).id) %>