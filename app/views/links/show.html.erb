<br>
<%= link_to @link_title, @link.source  %>
<% if current_user == @link.user %>
				| <%= link_to 'Edit', edit_link_path(@link) %> |  
				 <%= link_to 'Delete', @link, method: :delete, data: {confirm: 'Are you sure?'} %>
				 <% end %>
			
