<!--<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
             <a class="brand" href="/"><img src="https://s3.amazonaws.com/bklp/logo_biga.png"></a>

                <div class="nav-collapse">
                 <ul class="nav">
                 <div class="nav-srch">

                <!--<form role="search" class="search-form" action="/search" method="get">
                <label>
                    <input type="text" 
                    class="search-field" 
                    id="search_form"
                    placeholder="Find a book..."  
                    value="" name=":q"
                    title="Sök efter:">
                </label>
                    <input type="submit" class="search-submit" value="Search">
                </form>
                  
                 <div id="mod-search">
                    <%= form_tag("/search", method: "get", id: "search_form") do %>
                     <%= text_field_tag(:q) %>
                     <%= submit_tag("Find a book", class: "mag-glass", id: "search_btn") %>
                     <% end %>
                    <%#= link_to "+", new_link_path %>
                  </div>
                 </div>
                    </ul>

                    <ul class="nav pull-right">
                      <% if user_signed_in? %>
                         <nav onmouseover="">
                        <%= link_to 'Profile', current_user, :title => "Profile" %>
                        <%= link_to 'Settings', edit_user_registration_path, :title => "Settings" %>
                     </nav>
                      <% else %>

                    <li><%= link_to "Login", new_user_session_path %></li>
                      <% end %>
                          </ul>
                  </ul>
                </div>
        </div>
    </div>
</div>-->

<!-- main nav -->
<nav class="navbar navbar-default" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/"><img src="https://s3.amazonaws.com/bklp/logo_biga.png" alt="Bookloop"></a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <form class="navbar-form navbar-left" role="search">
                <div class="form-group">
                    <%= form_tag("/search", method: "get", id: "search_form") do %>
                        <%= text_field_tag(:q) %>
                    <% end %>
                </div>
            </form>
            
            <ul class="nav navbar-nav navbar-right">
                <% if user_signed_in? %>
                <li id="profile-name"><%= @user.name %></li>
                <li id="profile-img"><%= image_tag @user.avatar.url %></li>
                <li id="signout"><a href="/signout"><img src="./assets/img/signout.png"></a></li>
                <% else %>
                <li><%= link_to "Login", new_user_session_path %></li>
                <% end %>
            </ul>

        </div>
    </div>
</nav>