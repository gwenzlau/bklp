<div id="main">
	<div class="container">
		<div class="row">
			<div class="row">
				<%= render 'layouts/sidebar' %>
				<div class="col-md-8 col-md-offset-1" id="activity-feed">
					<% if @user.id == current_user.id %>
    				<label>Your Future Reads</label>
    				<% else %>
    				<label><%= @user.name %>'s Future Reads</label>
    				<% end %>
    				<div class="row" id="reads">
    					<% @future.each do |future| %>
    					<div class="col-md-2">
    						<%= link_to (image_tag ("http://images.amazon.com/images/P/#{future.isbn}.01.LZZZZZZ.jpg"), :title => "#{future.title}", data: { toggle: "tooltip"}), book_path(:id => future.olidb) %>
    					</div>
    					<% end %>
    				</div>
    			</div>
			</div>
		</div>
	</div>
</div>

<script>
	$('#reads img').tooltip({
		placement: 'bottom'
	});
</script>